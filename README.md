# rubocop-espago
Espago's style guide for Ruby.

## Installation

Add this line to your application's Gemfile:

```ruby
gem 'rubocop-espago'
```

And then to `.rubocop.yml`:

```yaml
inherit_gem:
  rubocop-espago:
    - config/default.yml
```
